def add_one(number: float) -> float:
    """Increment number by 1.

    Args:
        number: Value to increment.

    Returns:
        Value incremented by 1.

    Examples:
        >>> add_one(1)
        2

        >>> add_one(3.14)
        4.14
    """
    return number + 1
